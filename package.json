{
  "name": "finxia-capital-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf out && cd frontend-nextjs && yarn install && yarn build && cd .. && cp -r frontend-nextjs/out ./out",
    "dev": "cd frontend-nextjs && yarn dev"
  },
  "engines": {
    "node": "20"
  }
}
