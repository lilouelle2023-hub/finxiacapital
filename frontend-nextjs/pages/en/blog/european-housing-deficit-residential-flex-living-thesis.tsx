export { default } from '../../blog/deficit-logement-europe-these-residentielle-flex-living';

export async function getStaticProps() {
  return {
    props: {
      initialLanguage: 'en'
    }
  };
}
