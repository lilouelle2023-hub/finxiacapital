export { default } from '../../blog/repositionnement-hotelier-premium-europe-methode-brownfield';

export async function getStaticProps() {
  return {
    props: {
      initialLanguage: 'en'
    }
  };
}
