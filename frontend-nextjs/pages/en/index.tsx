// This is a symbolic link page that uses the same component as the French homepage
// The LanguageContext automatically detects /en/ in the URL and switches to English
export { default } from '../index';
